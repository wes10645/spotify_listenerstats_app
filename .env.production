# Faster, lower-memory production builds on Vercel (CRA reads this for `npm run build`)
GENERATE_SOURCEMAP=false
# Vercel sets CI=true; CRA treats ESLint warnings as errors. This avoids deploy failing on warnings.
DISABLE_ESLINT_PLUGIN=true
