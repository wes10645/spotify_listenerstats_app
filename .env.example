# Spotify OAuth (public client id — safe in the browser; use env so it is not tied to git / easy to rotate)
# Copy to .env.local for local builds. On Vercel: Project → Settings → Environment Variables (all environments).
REACT_APP_SPOTIFY_CLIENT_ID=
